#pragma once

class Persona{
   public:
      explicit Persona();
      virtual ~Persona();
      int x;
      int y;
      int height;
      int width;      

   // private:
      // Persona(const Persona&);
      // Persona& operator=(const Persona&);
};
